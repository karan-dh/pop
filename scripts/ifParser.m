// Matlab code to segment out the instruction formats.

ifs = imread('raw/instruction-formats-1.png');

// convert the image to b/w

// compute row sums.

// compute median points.

// possibly, try to extract the instruction format.

// write the subsections to file.